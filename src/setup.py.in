from distutils.core import setup

setup(
        name='skelet3d',
        version='${PACKAGE_VERSION}',
        package_dir={ '': '${CMAKE_CURRENT_SOURCE_DIR}'+"/src" },
        #packages=['skelet3d']
        py_modules=['skelet3d']
    )
